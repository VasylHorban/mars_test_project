import { PlanetDirective } from './planet.directive';

describe('PlanetDirective', () => {
  it('should create an instance', () => {
    const directive = new PlanetDirective();
    expect(directive).toBeTruthy();
  });
});
